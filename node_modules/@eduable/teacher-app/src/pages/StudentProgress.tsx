import React from 'react';
import { Box, Typography } from '@mui/material';

const StudentProgress: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Student Progress</Typography>
    <Typography variant="body1">Student progress and reports will appear here.</Typography>
  </Box>
);

export default StudentProgress;