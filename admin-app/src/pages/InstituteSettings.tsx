import React from 'react';
import { Box, Typography } from '@mui/material';

const InstituteSettings: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Institute Settings</Typography>
    <Typography variant="body1">Configure institute settings here.</Typography>
  </Box>
);

export default InstituteSettings;